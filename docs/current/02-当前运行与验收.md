# Aquafish 当前运行与验收

## 环境

- JDK 21
- Node.js 22
- pnpm 11

## 管理端

```powershell
cd admin
pnpm install --frozen-lockfile
pnpm test
pnpm build
```

## 服务端

```powershell
cd app
.\gradlew.bat test --no-daemon
```

## 统一验证

Windows 在仓库根目录运行：

```powershell
.\scripts\verify-build.bat
```

## 当前基线

2026-07-26 本地验证结果：

- Java：133 个测试套件，591 个测试，0 失败、0 错误、7 跳过。
- Admin：11 个测试文件，61 个测试全部通过。
- Admin 生产构建成功。

首次公开提交前仍需重新执行统一验证，并检查 Git 待提交文件中不存在运行数据、密钥和大体积构建产物。
