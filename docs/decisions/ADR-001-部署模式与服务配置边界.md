# ADR-001：部署模式与服务配置边界

> 状态：已接受。

## 决策

分发包安装器负责数据库三选一、Redis 可选和真实测试；Docker / 1Panel 托管部署跳过整个服务配置步骤。

## 原因

托管部署已经通过环境变量或平台配置确定服务，浏览器再次选择会造成二次配置、密码泄露和状态冲突。

## 后果

- 前端步骤由服务端可信上下文生成。
- URL 参数不能伪造托管模式。
- 分发包和托管接口分别测试，但共用数据库状态机。
