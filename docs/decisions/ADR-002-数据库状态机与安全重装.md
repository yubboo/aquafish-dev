# ADR-002：数据库状态机与安全重装

> 状态：已接受。

## 决策

首次安装、恢复和重新安装必须建立在后端数据库身份识别结果上。重新安装只删除 Aquafish 精确白名单表，不删除数据库。

## 安全边界

- STATE_UNAVAILABLE 默认拦截。
- 前端连接成功不能替代数据库身份识别。
- 两层用户确认。
- 正式执行前后端重新检查。
- 清理完成后必须确认回到 NEW_INSTALL。

## 被否决方案

- 仅凭网络连接成功进入下一步。
- 前端自行判断已有安装。
- 直接 DROP DATABASE。
- 在连接页内联展开危险确认。
